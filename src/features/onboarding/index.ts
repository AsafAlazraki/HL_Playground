/* ============================================================
   src/features/onboarding — public contract.
   One export: the two-step flow that names the organisation and
   picks its industry, then calls setOrganisation() and is done.
   CONFIGURATOR_SPEC.md §1a, §1b.
   ============================================================ */

export { Onboarding } from './Onboarding'
