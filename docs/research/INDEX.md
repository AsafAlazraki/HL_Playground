# RESEARCH INDEX

Every `/subtask` run lands here. The rule that makes this index worth keeping:
**a learning that lives only in a chat session is a learning that gets lost.**
This project lost a set of sessions once; the docs are what survived, so the
docs are where research goes.

| date | file | question it answers | status |
|---|---|---|---|
| 2026-09-08 | [ui-ux-world-class-2026.md](ui-ux-world-class-2026.md) | What do the best configurators and dense professional tools do, and are we doing it? | **incomplete** — grounding half only; re-run to finish |
| 2026-09-08 | [stack-and-practice.md](stack-and-practice.md) | Is the stack genuinely current, and where does engineering practice have gaps? | complete — stack current to the patch; 10 adoptions ranked, `check.sh` pipefail first |
| 2026-09-08 | [configurator-teardowns-2026.md](configurator-teardowns-2026.md) | When a combination does not work, what does a shipping configurator actually say? | complete |
| 2026-09-08 | [explaining-a-refusal.md](explaining-a-refusal.md) | How do mature constraint systems tell a person why nothing fits — and what do consumer products do instead? | complete |
| 2026-09-08 | [dense-tables-and-selection.md](dense-tables-and-selection.md) | What are the published numbers for row height, column limits, selection and peek in dense professional tools? | complete |
| 2026-09-08 | [price-framing-and-bundles.md](price-framing-and-bundles.md) | When a purchase has components, a bundle discount and a recurring cost, what number goes on screen? | complete |
| 2026-09-08 | [cascade-announcement-spec.md](cascade-announcement-spec.md) | When a pick changes or removes something already chosen, what does the app draw — and what in the code would have to fire it? | complete — spec; one open decision (the undo contradiction) left for a person |
| 2026-09-09 | [design-contract-audit.md](design-contract-audit.md) | Measured against `DESIGN_CONTRACT.md` §11 and the ten principles, what does the tree actually do — and what should a rebuild do first? | complete — 37 stylesheets swept at `530597d`; 9 ranked violations, 1 defect found (`--sp-7`), 5 stale checklist items, and one rule the two governing docs disagree on |
| 2026-09-09 | [visual-qa-2026-09-09.md](visual-qa-2026-09-09.md) | After the display tier landed, what do the screens actually measure — and what is still wrong? | complete — 12 screens driven in Chrome at 1280 and 1024, 1,450 text leaves; the tier reaches **one** screen (7.53× on the configurator, 2.36–3.96× everywhere else), 21 band names at 4.33:1, one mid-word truncation, 10 ranked findings |
| 2026-09-10 | [cascade-teardown-porsche-live.md](cascade-teardown-porsche-live.md) | The playbook called Porsche's cascade "the one to beat" from a single quoted line and never studied it. What is it actually? | complete — driven live; it is a ROUTE (`/feasibility-notification`) carrying the option, the resulting build and the build to restore on decline. Two cards, a chip each, footer prices the DECISION not the click. Every removed row says the same sentence and names neither side — the gap we close. Built as `features/quote/cascade.ts` + `CascadeSheet.tsx` |
| 2026-09-10 | [motion-libraries-2026.md](motion-libraries-2026.md) | magicui, reactbits, threlte, vectory, jitter — asked for by name. Which components actually belong in a tool used all day? | complete — threlte is Svelte and cannot run here; reactbits is 27 cursor/background effects, none fit; magicui is 100 components of which **three** do. Border Beam was ported, corrected twice and then **deleted on measurement** (a 9–15% wash over an already-washed head is invisible, and §4b caps the fix). The rail-edge fade shipped |
| 2026-09-10 | [godly-and-what-transfers.md](godly-and-what-transfers.md) | godly.website — what do the best-designed sites do, and which of it survives a dealer, a customer beside them and a 15,691-row price file? | complete — godly.website now redirects to recent.design; **one of its eleven categories is operable software**. Yield is low and structurally so: a gallery optimises for the screenshot, a tool is judged on the four-hundredth press. The best idea there (colour swatches for variants) is **rejected on data** — the workbook has no colour column and decoding `B-G-B` would be the guess §7 forbids. We already solve it with the photograph |
| 2026-09-12 | [motion-libraries-remine-2026-09-12.md](motion-libraries-remine-2026-09-12.md) | The 2026-09-10 verdicts named 9 of "27" reactbits and ~15 of "100" magicui. Re-mined against `DESIGN_SYSTEM.md` — what does the full list actually hold? | complete — **both denominators were also wrong**: reactbits is **171** (llms.txt, confirmed against sitemap.xml) and magicui is **78**, not 27 and 100. All **249** named and gated. **24 PORT-SHOWROOM · 5 PORT-COCKPIT · 60 PORT-MAYBE · 160 REJECT**, every reject with a product reason. The rejections still land where the old sample looked (76 of 94 reactbits Animations+Backgrounds) and the yield is in the Components section nobody opened. recent.design looked at properly: Typography holds **3** posts, one of which is a tool and transfers; Motion holds **237**, of which ~85 are interface microinteractions with named prior art for the cascade, stage transitions and the step rail. Ranked build list led by the step rail (§9.6), the Chroma-Grid-inverted variant picker, and Animated Beam + Highlighter for the cascade. Corrects one first-hand: `build.css:796` is a **mask fade, not a Progressive Blur**, and the file argues against the blur |
| 2026-09-13 → 15 | [visual-qa-rebuild.md](visual-qa-rebuild.md) | The rebuild's own scoreboard, one entry per pass, against `visual-qa-2026-09-09.md`'s rulers. What does every screen actually measure? | complete and kept current — every screen in the inventory (`docs/specs/SCREENS.md`) has an entry; the last three (2026-09-15, night) are the tile rule on home, the catalogue and modules, the document's cover, and the entry (sign-in, the wizard, the first run, on Porsche's login frame). Standing figures at `3c0e8e1`: seven guards green, 2,974 tests, lint 343/343, literal px 460/460; nothing overlaps at 1440×900; 1,278 text nodes over eleven screens clear contrast; sweep clean; 14 shot baselines. References driven live are kept in `out/ref/`, `out/ref/boats/` and `out/ref/entry/` (not committed) |
| 2026-09-14 | [helmlogic-original.md](helmlogic-original.md) | The original HelmLogic went public. Read end to end by three agents: the PandaDoc-style document editor, the gap against this tree, and every interaction worth taking. | complete — v1.33.1, ~96k lines, 33 releases. The document editor is a **fixed-slot composer, not a block canvas**: 7 authored sections around 4 locked ones, and its best idea is a **four-layer override cascade** (quote → brand → org → default) with an admin lock that short-circuits it. Its drag-to-reorder **has never reached the customer PDF**. Ten ranked gaps led by authored prose on the document, stock-as-rows with a by-reference allocation, and a person in the model; ten patterns to steal outright led by inline-edit-in-place, import-with-diff-preview and the Step-5 relevance doctrine (narrow, name the rule, count what you hid, always offer Show all). Their storage.rules is world-writable and their signup page has a self-service superadmin checkbox: **migrate the model, never the enforcement.** Where we are already better is named too, including that a grep of their whole `src/` for `localStorage` returns zero hits |
